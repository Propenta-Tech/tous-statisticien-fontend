"use client"
// LoginForm - À développer
interface LoginFormProps {
  // Props à définir
}

export const LoginForm: React.FC<LoginFormProps> = (props) => {
  return <div>LoginForm</div>
}

export { LoginForm }
