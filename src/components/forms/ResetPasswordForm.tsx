"use client"
// ResetPasswordForm - À développer
interface ResetPasswordFormProps {
  // Props à définir
}

export const ResetPasswordForm: React.FC<ResetPasswordFormProps> = (props) => {
  return <div>ResetPasswordForm</div>
}

export { ResetPasswordForm }
