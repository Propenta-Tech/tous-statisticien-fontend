"use client"
// ResourceForm - À développer
interface ResourceFormProps {
  // Props à définir
}

export const ResourceForm: React.FC<ResourceFormProps> = (props) => {
  return <div>ResourceForm</div>
}

export { ResourceForm }
