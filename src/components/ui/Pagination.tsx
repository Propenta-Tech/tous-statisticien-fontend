"use client"
// Pagination - À développer
interface PaginationProps {
  // Props à définir
}

export const Pagination: React.FC<PaginationProps> = (props) => {
  return <div>Pagination</div>
}

export { Pagination }
