import { create } from 'zustand'

// searchStore - À développer
interface SearchStoreState {
  // État à définir
}

export const useSearchStore = create<SearchStoreState>((set) => ({
  // À implémenter
}))
