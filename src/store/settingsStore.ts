import { create } from 'zustand'

// settingsStore - À développer
interface SettingsStoreState {
  // État à définir
}

export const useSettingsStore = create<SettingsStoreState>((set) => ({
  // À implémenter
}))
