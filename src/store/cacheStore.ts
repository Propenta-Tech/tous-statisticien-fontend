import { create } from 'zustand'

// cacheStore - À développer
interface CacheStoreState {
  // État à définir
}

export const useCacheStore = create<CacheStoreState>((set) => ({
  // À implémenter
}))
