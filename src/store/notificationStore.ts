import { create } from 'zustand'

// notificationStore - À développer
interface NotificationStoreState {
  // État à définir
}

export const useNotificationStore = create<NotificationStoreState>((set) => ({
  // À implémenter
}))
